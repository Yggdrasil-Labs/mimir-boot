# 质量评分

本文档定义本仓库当前采用的质量观察维度，用于帮助开发者和智能体判断哪些区域需要优先补强。

## 0. 使用边界

本文件是人工维护的“主观观察快照”，不是自动生成的质量报表。

在使用这份文档时，应同时注意：

- 它反映的是某个时间点的人工判断
- 它不能替代测试、CI、覆盖率和代码审查
- 如果没有及时更新，分数会快速失真

建议每次大版本演进、重要模块重构或文档体系显著变化后回看一次。

## 1. 评分对象

评分对象不是单纯的代码行数，而是“模块或文档在工程维度上的成熟度”。

## 2. 建议维度

每个维度可按 `A / B / C / D` 粗粒度打分：

| 维度 | 含义 |
|---|---|
| 正确性 | 行为是否稳定、是否有测试支撑 |
| 可读性 | 代码与文档是否容易导航和理解 |
| 可维护性 | 是否有清晰边界、低耦合、易扩展 |
| 文档完整性 | 是否有 README、索引、边界说明 |
| 发布成熟度 | 是否已纳入 CI、发布、版本策略 |

## 3. 当前观察

评估时间：2026-04-11  
评估方式：基于当前仓库结构、README 覆盖度、CI/发布事实和文档完整性的人工判断。  
维护责任：进行结构性改动的人应同步校准本表，避免把过期印象保留下来。

| 区域 | 正确性 | 可读性 | 可维护性 | 文档完整性 | 发布成熟度 | 备注 |
|---|---|---|---|---|---|---|
| `mimir-boot-parent` | A | B | A | B | A | 工程规则较完整，仍可加强架构文档联动。 |
| `mimir-boot-bom` | A | B | A | B | A | 版本矩阵清晰，但缺少与规划能力的映射说明。 |
| `mimir-boot-common` | B | B | B | B | A | 公共抽象较集中，需持续防止工具箱膨胀。 |
| 已落地 starters | B | B | B | B | A | README 较多，但跨模块总览此前较弱。 |
| `docs/` 文档体系 | B | A | B | B | B | 本轮已补骨架，后续需增加自动校验与持续维护。 |

## 4. 使用方式

当你准备发起改动时，可先问：

- 这个区域当前最弱的维度是什么？
- 本次改动是否应该顺手补齐文档、测试或边界说明？

## 5. 维护建议

- 每次大型演进后回看本评分
- 如果某区域长期停留在 `C` 或 `D`，应形成明确计划
- 不要为了“评分好看”而制造空文档，分数应反映真实可用性
- 如果无法给出明确依据，宁可把评分写成“待复核”，也不要伪装成确定事实
